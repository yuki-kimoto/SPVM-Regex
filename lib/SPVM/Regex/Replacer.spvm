# Copyright (c) 2023 Yuki Kimoto
# MIT License

class Regex::Replacer : interface_t {
  version_from Regex;
  
  use Regex;
  use Regex::Match;
  
  required method : string ($re : Regex, $match : Regex::Match);
}
